<form method="post" action="{{ path('app_bizum_delete', {'id': bizum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bizum.id) }}">
    <button class="btn">Delete</button>
</form>
