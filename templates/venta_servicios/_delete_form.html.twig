<form method="post" action="{{ path('app_venta_servicios_delete', {'id': venta_servicio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ venta_servicio.id) }}">
    <button class="btn">Delete</button>
</form>
