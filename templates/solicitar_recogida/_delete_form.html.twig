<form method="post" action="{{ path('app_solicitar_recogida_delete', {'id': solicitar_recogida.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solicitar_recogida.id) }}">
    <button class="btn">Delete</button>
</form>
