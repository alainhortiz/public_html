<form method="post" action="{{ path('app_paypal_delete', {'id': paypal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paypal.id) }}">
    <button class="btn">Delete</button>
</form>
